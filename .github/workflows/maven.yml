name: Java CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: GitHub Actions for Apache Maven
      uses: actions/checkout@v3
    - run: |
        mvn clean install
        mvn test
        mvn site
