name: Java CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - name: GitHub Actions for Apache Maven
      uses: actions/checkout@v5
    - run: |
        mvn clean install
        mvn test
        mvn site
